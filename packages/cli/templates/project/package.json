{
  "name": "{{PROJECT_NAME}}",
  "version": "0.1.0",
  "private": true,
  "description": "A Busara multi-agent data analysis project",
  "scripts": {
    "dev": "busara serve --port 3000",
    "analyze": "busara analyze",
    "agents": "busara agents list"
  },
  "dependencies": {
    "@busara/agents": "{{BUSARA_VERSION}}",
    "@busara/cli": "{{BUSARA_VERSION}}",
    "@busara/core": "{{BUSARA_VERSION}}"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
